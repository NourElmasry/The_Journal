@model IEnumerable<The_Journal.Models.Family>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Account.Email)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Account.Email)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.FamilyID }) |
                @Html.ActionLink("Details", "Details", new { id=item.FamilyID }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.FamilyID })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
